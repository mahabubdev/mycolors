import React from 'react';
import './css/header.css';

const Header = (props) => {
    return(
        <div className="header-area">
            <h2>Header Area</h2>
        </div>
    );
}

export default Header;